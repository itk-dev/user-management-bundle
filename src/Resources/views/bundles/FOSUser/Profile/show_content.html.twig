{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show">
  <div class="form-group">
    <label class="col-sm-2 control-label required">{{ 'profile.show.email'|trans }}</label>
    <div class="col-sm-10">{{ user.email }}</div>
  </div>
  <div class="form-group">
    <a class="btn btn-default" href="{{ path('fos_user_change_password') }}">{{ 'change_password.submit'|trans({}, 'FOSUserBundle') }}</a>
  </div>
</div>
